# CyberGuard API — pinned runtime dependencies (M-4).
# Target runtime: CPython 3.11 or 3.12 (see ../.python-version, ../pyproject.toml).
#
# This file is the pin source. After changing any line, regenerate a fully
# hashed lock in a clean 3.12 venv:
#     python -m venv .venv && .venv/bin/pip install -r cyberguard_api/requirements.txt
#     .venv/bin/pip freeze --all > cyberguard_api/requirements.lock
#
# CVE / freshness notes:
#   * pyjwt bumped 2.8.0 -> 2.10.1 -> 2.13.0 (clears the 2026 alg-confusion /
#     JWK-parsing advisories; API-compatible, HS256 encode/decode only).
#   * cryptography bumped 43.0.1 -> 50.0.1 (clears PYSEC-2026-35/1284/2141/
#     3552/3553/3554 + GHSA-537c-gmf6-5ccf). Only used for offline Ed25519
#     manifest-signature verification; the primitives are unchanged since 2.x.
#   * starlette / fastmcp / mcp carry advisories whose fixes require breaking
#     major upgrades (fastapi<->starlette coupling; fastmcp 2->3/4 rewrite).
#     Deferred to a dedicated dependency-bump PR with full runtime validation;
#     the specific IDs and rationale are tracked in ../.pip-audit-ignore.

# --- web framework ---
fastapi==0.115.0
starlette==0.38.6
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# --- gateway / edge (C-3) ---
slowapi==0.1.9

# --- observability (PP-H4) ---
prometheus-client==0.21.0

# --- model manifest signature verification (PP-M1, optional-but-pinned) ---
cryptography==50.0.1

# --- ML stack ---
scikit-learn==1.6.1
pandas==2.2.3
numpy==2.1.2
joblib==1.4.2

# --- runtime deps ---
psutil==5.9.8
httpx==0.28.1
pyjwt==2.13.0
redis==5.0.1

# --- MCP stdio server ---
fastmcp==2.3.4
